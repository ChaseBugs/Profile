# Proposal — SurvNet (RKI) XML Transport / IfSG Data Transmission Expert

---

## Opening

I'm a backend developer with 7+ years of experience in Java and C# (.NET), with a strong background in **healthcare IT systems** — including EHR integrations, lab data pipelines, and structured clinical data exchange. I've worked extensively with XML-based data transport, schema validation (XSD), and standards-driven data formats in regulated health environments.

I understand this role requires hands-on SurvNet experience, specifically around XML transport files and RKI XSD validation. I want to be transparent: my SurvNet exposure is from the integration side — working with systems that produce or consume IfSG-structured data — rather than deep SurvNet admin experience. However, the core technical tasks described map directly to work I've done:

---

## Why I Fit This Task

### 1. XML Transport Files — Creating & Adjusting
I have hands-on experience building and manipulating structured XML payloads for health data exchange. In Java (JAXB, DOM, SAX) and C# (.NET XmlSerializer, XDocument), I've built pipelines that generate, transform, and transmit XML documents between clinical systems — including HL7, FHIR-adjacent formats, and custom schema-bound formats.

Creating or adjusting SurvNet XML transport files is within my direct skill set.

### 2. XSD Validation & Schema Compliance
I've worked with XSD-bound validation in multiple healthcare projects — validating documents against institutional schemas, resolving namespace mismatches, encoding issues (UTF-8 BOM problems, special character handling), and structural errors flagged by schema validators. This is exactly the kind of work described.

### 3. .NET Environment
SurvNet runs on a .NET-based stack. I work daily in C# / ASP.NET Core and understand how .NET handles XML serialization, encoding, and schema binding — which is often where subtle transport issues arise.

### 4. Healthcare Regulatory Context
I've delivered backend systems in regulated healthcare environments (EU and US). I understand the importance of data accuracy, auditability, and compliance-first thinking — which matters in an IfSG/RKI reporting context.

---

## What I'll Deliver in 1–3 Days

| Day | Deliverable |
|-----|-------------|
| **Day 1** | Review existing XML transport files + XSD specs. Identify structural, encoding, or schema issues. Produce a written gap analysis. |
| **Day 2** | Create corrected / adjusted XML files. Validate against RKI XSD. Resolve all validation errors. Test import flow. |
| **Day 3** | Document the transport mechanism, import process findings, and any edge cases for existing case updates. Handover. |

---

## Questions I'd Ask Before Starting

To hit the ground running on day one, I'd want to clarify:

1. Which SurvNet version is in use? (affects XSD schema version)
2. Are the XML files being generated by an existing system, or created from scratch?
3. What validation errors are currently occurring? (error messages / XSD validator output)
4. Is the goal to import into **existing** SurvNet cases (case update/merge), or create new notifications?
5. Is there access to a SurvNet test/staging environment for validation?

---

## Availability

- Available to start immediately
- Full remote — async or real-time collaboration (EU time zones compatible)
- Rate: open to discuss based on confirmed scope

---

## Honest Note

I want to be direct: if your requirement is someone who has logged hours inside the SurvNet GUI and knows its import dialogs by memory, I may not be the fastest start. But if the core problem is **XML structure, XSD validation, encoding, and transport logic** — that is exactly the kind of technical problem I solve. I ramp fast on domain-specific specs and deliver working outputs, not theoretical advice.

Happy to do a short call to assess fit before committing.

---

*Based on profile: Java + C# (.NET) Backend Developer | Healthcare IT | 7+ years*
